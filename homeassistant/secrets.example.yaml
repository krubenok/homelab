# Dummy secrets file for local validation/CI.
# On the HA VM, the real `secrets.yaml` lives in /config and is not committed.
#
# Add keys here as you migrate config values to `!secret`.

example_api_key: "replace-me"

